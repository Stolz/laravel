@extends('layouts.app')

@section('page.title', _('Create dummy'))

@section('main')
<div class="row justify-content-md-center">
    <div class="col col-md-10 col-lg-4">

        <form method="post" action="{{ route('bogus.dummy.store') }}" role="form">
            @csrf
            @include('modules.bogus.dummy.form')

            <div class="row">
                @can('list', 'App\Models\Dummy')
                    <div class="col">
                        <a href="{{ previous_index_url(route('bogus.dummy.index')) }}" class="btn btn-outline-secondary btn-block">{{ _('Cancel') }}</a>
                    </div>
                @endcan
                <div class="col">
                    <button type="submit" class="btn btn-primary active btn-block">{{ _('Create dummy') }}</button>
                </div>
            </div>
        </form>

    </div>
</div>
@stop
